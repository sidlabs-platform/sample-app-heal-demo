{
  "name": "sample-app-heal-demo",
  "version": "1.0.0",
  "description": "Sample Node.js app demonstrating the Auto-Heal CI Agent GitHub Action",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "test": "jest --verbose",
    "lint": "eslint src/ tests/",
    "lint:json": "eslint src/ tests/ --format json --output-file lint-output.json || true",
    "test:json": "jest --json --outputFile=test-results.json || true"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^5.2.1"
  },
  "devDependencies": {
    "eslint": "^9.39.3",
    "jest": "^30.2.0",
    "supertest": "^7.2.2"
  }
}
